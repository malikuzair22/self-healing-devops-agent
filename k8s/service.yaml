apiVersion: v1
kind: Service
metadata:
  name: target-app-service
  namespace: default
  labels:
    app: target-app
spec:
  selector:
    app: target-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: NodePort